#### Gson的 @SerializedName
> 1.使用Gson转换json串和javaObject时,要求字段名与key值相同,使用@SerializedName("name"),name为json中的键值,这样就不用让字段名必须与key值保持一样了
> 2.@SerializedName(value = "name",alternate = {"name_new"})  name和name_new同时存在匹配最后一个,只有一个存在也会可以匹配